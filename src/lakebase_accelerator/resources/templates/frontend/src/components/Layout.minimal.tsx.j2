import React from 'react'
import { Outlet, NavLink } from 'react-router-dom'

const Layout: React.FC = () => {
  return (
    <div className="app-layout app-layout-minimal">
      <header className="minimal-header">
        <h1 className="minimal-brand">{{ project_title }}</h1>
        <nav className="minimal-nav">
          <NavLink
            to="/"
            end
            className={({ isActive }) => `minimal-link${isActive ? ' active' : ''}`}
          >
            Home
          </NavLink>
{% for entity in entities %}
          <NavLink
            to="/{{ entity.plural }}"
            className={({ isActive }) => `minimal-link${isActive ? ' active' : ''}`}
          >
            {{ entity.display_name_plural }}
          </NavLink>
{% endfor %}
        </nav>
      </header>
      <main className="main-content main-content-minimal">
        <Outlet />
      </main>
    </div>
  )
}

export default Layout
