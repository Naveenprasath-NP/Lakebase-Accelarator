"""Database schema definitions for the accelerator_meta tables.

Each file defines the DDL and column structure for a single table.
These are used by schema_init.py for table creation and by repositories
for type-safe column references.
"""
